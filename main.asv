%%

clear all;
clc;
close all;

% this is a code for wet 3 of DSP

Fs = 1000; % original FS

%% Section 1

% ideal filter prototype

L = 9;
fsamp = Fs*L;
M = 1000;
fstop = 1.3/L;
fpass = 0.7/L;

% h = fdesign.lowpass('N,Fp,Fst',M,fpass,fstop,fsamp);
lpFilt = designfilt('lowpassfir','PassbandFrequency',fpass, ...
         'StopbandFrequency',fstop);
fvtool(lpFilt)

theta = 0:0.001*pi:pi;

lpFilt = (